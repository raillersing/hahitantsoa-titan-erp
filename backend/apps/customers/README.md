# Customers

F99 introduces the minimal customer/contact foundation for the MVP.

The app exposes authenticated read-only customer endpoints and a local demo seed
command. It does not implement customer creation through the API, reservations,
billing, payments, contracts, invoices, document generation, pricing, stock,
quantities or a commercial workflow.

Customer data is introduced now because future reservations and documents need a
stable customer/contact reference.
